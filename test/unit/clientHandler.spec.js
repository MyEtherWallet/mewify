/* eslint-env mocha */

var path = require('path'),
    sinon = require('sinon'),
    chai = require('chai'),
    assert = chai.assert,
    srcRoot = path.resolve(__dirname + '/../../src'),
    ipcProvider = require(srcRoot + '/scripts/libs/ipcProvider'),
    httpProvider = require(srcRoot + '/scripts/libs/httpProvider');



describe('clientHandler', function() {
  before(function() {

  });

  after(function() {

  });
});
